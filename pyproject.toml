[project]
name = "melo-ai"
version = "0.1.0"
description = "AI app that generates role-specific interview questions for a job title using the Google Gemini API."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
